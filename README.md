Goals:

- Importable app profiles for each model/manufacturer/type
- Generic profiles offered; non-submittable, few tags defined
- Empty tags offered like electricity= or camera:mount= or note= where appropriate
- Each type/manufacturer/model should get some category tags like "LPR" or "temporary" or "traffic control" etc
- At least two images for each; types and manufacturers can of course reuse
